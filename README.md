# ChefOS developer docs

This repository contains the Mintlify documentation site for ChefOS, published at https://developer.chefos.us.

## Local development

Install the Mintlify CLI and run the preview from this directory:

```bash
npm i -g mint
mint dev
```

Validate links before publishing:

```bash
mint broken-links
```
